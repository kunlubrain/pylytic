# https://learnpython.com/blog/python-requirements-file/
# Create new environment in console:
# 1. Navigate to repository folder with requirements.txt in it
# 2. Execute: conda create --name pylytic --file requirements.txt python=3.10 -c conda-forge
# remove next for production
black==22.10.0
# remove next for production
flake8==5.0.4
# remove next for production
ipykernel==6.19.2
pandas==1.5.1
# remove next for production
pre-commit==2.20.0
prophet==1.1.1
# remove next for production
pytest==7.1.2
scikit-learn==1.2.0
